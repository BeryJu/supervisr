version: '3'

services:
  db:
    image: mariadb:latest
    environment:
      - MYSQL_RANDOM_ROOT_PASSWORD=yes
      - MYSQL_DATABASE=supervisr
      - MYSQL_USER=supervisr
      - MYSQL_PASSWORD=EK-5jnKfjrGRm<77
  redis:
    image: redis:latest
  supervisr:
    image: supervisr
    build:
      context: ../..
      dockerfile: build/docker/Dockerfile
    command: build/docker/start_wrapper.sh "inv ci.unittest"
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    depends_on:
      - db
      - redis
