# Env for Docker images
databases:
  default:
    engine: django.db.backends.mysql
    name: supervisr
    user: supervisr
    password: 'EK-5jnKfjrGRm<77'
    host: localhost
    port: ''
    options:
      charset: UTF8MB4
      sql_mode: 'STRICT_TRANS_TABLES'
      init_command: "ALTER DATABASE supervisr CHARACTER SET utf8 COLLATE utf8_unicode_ci;"
redis: localhost
debug: false
