{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "supervist-ctl: env.isort",
            "command": "sv env.isort",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        },
        {
            "type": "shell",
            "label": "supervist-ctl: env.lint",
            "command": "sv env.lint",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}
